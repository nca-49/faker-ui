export enum ButtonVariant {
  DEFAULT = "default",
  OUTLINE = "outline",
}

export enum InputType {
  TEXT = "text",
  NUMBER = "number",
  EMAIL = "email",
}

export enum ButtonLabel {
  COPIED = "Copied !",
  COPY = "Copy",
  GENERATE = "Generate",
}
